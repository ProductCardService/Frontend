interface GeneratedImages {
    images: Array<string>;
}

interface GeneratedDescriptions {
    descriptions: Array<string>;
}

interface GeneratedTags {
    tags: Array<string>;
}

interface Prompt {
    title: string;
}

export type {GeneratedImages, GeneratedTags, GeneratedDescriptions, Prompt}